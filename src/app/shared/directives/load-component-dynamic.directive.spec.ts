import { LoadComponentDynamicDirective } from './load-component-dynamic.directive';

describe('LoadComponentDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadComponentDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
