export enum FormParts {
  FormGroup = '1',
  FormControl = '2',
  FormArray = '3'
}

export enum ContactFields {
  firstName = 'firstName',
  lastName = 'lastName',
  phoneNumber = 'phoneNumber',
  countryCode = 'countryCode',
  emailAddress = 'emailAddress',
  image = 'image'
}
