export enum FormParts {
  FormGroup = '1',
  FormControl = '2',
  FormArray = '3'
}

export enum ContactFields {
  firstName = 'firstName',
  lastName = 'lastName',
  userName = 'userName',
  mobileNumber = 'mobileNumber',
  countryCode = 'countryCode',
  email = 'email',
  image = 'image'
}
